$kind: http-request
name: Update Uploaded File
method: PUT
url: '{{baseUrl}}/api/uploadedfiles/{{id}}'
order: 4000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "uploadedFileId": 0,
      "fileName": "string",
      "filePath": "string",
      "fileSize": 0,
      "fileType": "string"
    }
events:
  - listen: test
    script:
      type: text/javascript
      exec: |-
        pm.test("Status code is 204", () => {
            pm.response.to.have.status(204);
        });

        pm.test("Response body is empty", () => {
            pm.expect(pm.response.text()).to.be.empty;
        });
